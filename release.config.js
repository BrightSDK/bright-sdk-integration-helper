// LICENSE_CODE ZON

module.exports = {
  // Look for the minified output from our build process
  artifactsPattern: 'dist/*.min.js'

  // All other options use sensible defaults:
  // - outputDir: 'releases'
  // - versionDirectories: ['major', 'minor', 'patch', 'latest']
  // - preserveDirectory: false
  // - generateManifest: true
};
